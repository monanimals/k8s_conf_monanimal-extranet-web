apiVersion: v1
kind: Service
metadata:
  name: service-monanimal-extranet
spec:
  type: ClusterIP
  selector:
    app: monanimal-extranet
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  - name: jmx 
    port: 80
    targetPort: 8080
